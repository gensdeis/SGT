using UnityEngine;
using ShortGeta.Core.UI;

namespace ShortGeta.UI.PC
{
    // PC (Steam 클라이언트 스타일) 홈 화면.
    //
    // 목업 (shortgeta-plan-v1.3.html) 기준 레이아웃:
    //   ┌──────┬──────────────────────────────┐
    //   │ 로고 │ [검색바]            [랭킹][시작]│
    //   │      ├──────────────────────────────┤
    //   │ 메뉴 │ [피처드 카드 — 신작/추천]      │
    //   │ 홈   ├──────────────────────────────┤
    //   │ 인기 │ 취향 맞춤 (5열 그리드)          │
    //   │ 보관함├──────────────────────────────┤
    //   │ 리더 │ 이번 주 핫한 (3개 1/2/3위)     │
    //   │ 설정 │                                │
    //   ├──────┤                                │
    //   │ 태그 │                                │
    //   │ 필터 │                                │
    //   ├──────┤                                │
    //   │ 프로 │                                │
    //   │ 필   │                                │
    //   └──────┴──────────────────────────────┘
    //
    // TODO Iter 5+:
    //   - 좌측 사이드바 (170px 고정 width)
    //   - 5열 그리드 게임 카드 (9:16 thumbnail)
    //   - 윈도우 chrome (signal red/yellow/green dot)
    //   - 태그 필터 (사이드바 하단 색상 dot)
    //   - 리더보드 미리보기 패널
    //
    // 본 stage 는 placeholder 만 — 검정 화면 + 안내 텍스트.
    public class PcHomeController : MonoBehaviour
    {
        public void Build(Transform canvasRoot)
        {
            var root = UIBuilder.Panel(canvasRoot, "PcHomePanel",
                Vector2.zero, Vector2.one, DesignTokens.Bg);

            UIBuilder.Label(root.transform,
                "PC UI — 곧 나옵니다 (현재 모바일 UI 우선 개발 중)",
                DesignTokens.FontH2, DesignTokens.TextDim,
                TMPro.TextAlignmentOptions.Center,
                anchorMin: new Vector2(0.1f, 0.45f), anchorMax: new Vector2(0.9f, 0.55f));
        }
    }
}
