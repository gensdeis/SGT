apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Real 클러스터 — 미구축. 출시 시점에 채울 것.
# - replicas 2+
# - 외부 관리형 DB
# - cert-manager + Traefik Ingress
# - SealedSecret

namespace: shortgeta-real

resources:
  - ../../base
